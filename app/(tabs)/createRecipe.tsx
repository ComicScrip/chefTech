import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function CreateRecipeScreen() {
	return (
		<SafeAreaView>
			<Text>Ecran création d'une recette</Text>
		</SafeAreaView>
	);
}
